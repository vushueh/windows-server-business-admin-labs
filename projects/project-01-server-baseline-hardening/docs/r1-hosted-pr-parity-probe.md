# R1 Hosted Pull-Request Parity Probe

This temporary, documentation-only file exists only to trigger the public
repository's `P01 Safety Check` through the `pull_request` event.

It changes no workflow, server, identity, policy, secret, or network setting.
The test branch will be closed and deleted after the GitHub-hosted run passes.
